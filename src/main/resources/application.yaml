spring:
  application:
    name: CellRepair
  jpa:
    show-sql: true
  datasource:
    url: jdbc:mysql://localhost:3306/cellrepair_test
    username: root
    password: root
    driver-class-name: com.mysql.cj.jdbc.Driver
  sql:
    init:
      mode: always

api:
  security:
    token:
      secret: ${JWT_SECRET:my-secret-key}